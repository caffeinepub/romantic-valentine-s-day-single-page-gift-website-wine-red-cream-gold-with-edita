// This file is intentionally minimal as the backend has no methods
// All state is managed locally in the frontend components
// The publishDraftToProduction method is called directly via useActor in the PublishDraftBar component
export function useQueries() {
  return {};
}
